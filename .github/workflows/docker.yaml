name: 'Docker Build and Push'

on:
  workflow_dispatch:

  push:
    branches:
      - main

  pull_request:
    branches:
      - main

jobs:
  helm_build_push:
    uses: mars-office/.github/.github/workflows/docker.yaml@main
    secrets: inherit
